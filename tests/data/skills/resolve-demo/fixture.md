# Fixture File

MARKER: FIXTURE_FILE_RESOLVED_OK

This text should appear inside the attached `<file path="fixture.md">` context
message when `resolve-demo` is expanded. If you can read this marker in the
model-facing context, `@file` resolution for skills works.
